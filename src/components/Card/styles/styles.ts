import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
       width: 126,
        height:100,
        borderWidth:0.3,
        justifyContent:'center',
        alignItems:'center'
    },
    text:{
        fontWeight:'bold'
    }
})

export { styles }