import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        flex:1,
        justifyContent:'center',
        alignItems:'center'
    },
    wrapperContent:{
        flexDirection:'row',
        flexWrap: 'wrap',
        justifyContent:'center',
    },

})

export { styles };